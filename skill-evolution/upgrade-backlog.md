# Upgrade Backlog

Use this as a parking lot for future improvements.

## Planned

- Normalize Vietnamese notes that currently show mojibake in terminal output.
- Add more workshop lessons as separate raw files in `knowledge-from-workshop/`.
- Add forward-test prompts for each skill module.
- Add project templates for specs, ADRs, contracts, tasks, context summary, and DoD.
- Decide whether to package the modular skills as a plugin after several real projects.

## Open Decisions

- Which subagents should be enabled by default for daily projects?
- Should coder agents be split into `coder-mini` and `coder-hard`?
- Should enterprise workflow be a separate plugin later?
- Should project maturity use named modes, multi-dimensional scope profiles, or both?
